<variablelist
   xmlns="http://docbook.org/ns/docbook"
   xmlns:xi="http://www.w3.org/2003/XInclude">

  <title>Common Input/Output Options</title>
  
  <varlistentry>
    <term><option>-i <replaceable>file</replaceable></option></term>
    <listitem>
      <para>
        The input term given by the file name <filename><replaceable>file</replaceable></filename>.
      </para>
      
      <para>
        In the absence of the <option>-i</option> option, input
        will be read from <filename>stdin</filename>.
      </para>
    </listitem>
  </varlistentry>
  
  <varlistentry>
    <term><option>-o <replaceable>file</replaceable></option></term>
    
    <listitem>
      <para>
        The output will be written to the file given by the file name 
        <filename><replaceable>file</replaceable></filename>.
      </para>
      
      <para>
        In the absence of the <option>-o</option> option, 
        output will be written to <filename>stdout</filename>.
      </para>
    </listitem>
  </varlistentry>
  
  <varlistentry>
    <term><option>-b</option></term>
    
    <listitem>
      <para>
        The output will be written in the binary (BAF) ATerm format.
      </para>
      
      <para>
        ATerms in the BAF format require a lot less space than ones in the TAF format,
        but the Java ATerm library does not currently support baf ATerms. ATerms in the baf
        format is the preferred format of exchange between Stratego tools.
      </para>
    </listitem>
  </varlistentry>
</variablelist>
